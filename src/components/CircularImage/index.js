import { StyleSheet, Text, View ,Image} from 'react-native'
import React from 'react'
import { Image, } from 'react-native'

const CircularImage = () => {
  return (
    <View>
      <Text>CircularImage</Text>
    </View>
  )
}

export default CircularImage

const styles = StyleSheet.create({})